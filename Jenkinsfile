pipeline
{
agent {
  label 'newnode1'
}
stages {
  stage('c project') {
    steps {
      echo " new file is added"
    }
  }
}
stages {
  stage('build') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}
stages {
  stage('deploy') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}
stages {
  stage('test') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}


}

