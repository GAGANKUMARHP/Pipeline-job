/* groovylint-disable TrailingWhitespace */
pipeline 
{
    agent none
    stages{
     stage('Stage 1') {
       agent any {
        if (isMaster()) {
        return 'master' // Only run on master node
        }
       }
   
     }
    }

}